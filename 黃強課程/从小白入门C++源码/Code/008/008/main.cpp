#include<iostream>//C++的头文件，不带.h
#include<cstdio> // C的头文件，去掉.h，前面加上c
#include "008.h"  //如果是自定义头文件，需要加.h
#include "008.h"  //重复包含头文件
using  namespace  std;


int  main()
{
	 
	 cout << a << endl;

	 cout << sum(111, 222) << endl;

	 f(a);
	 cout <<a << endl;

	 cout << b << endl;


	return 0;
}