/*
让系统产生一个200 0000字节的栈内存，分配一个200 0000字节的堆内存，观察效果！
 */
#include <stdio.h> 

 
void   main()
{

    //200 0000字节的栈内存,会超出栈容量导致错误
    int a[500000] = { 0 };


    //200 0000字节的栈内存，正常
    int* p = (int*)malloc(2000000);
    *p = 100;
     free(p);
}
